{% extends 'base.html.twig' %}

{% block body %}
  <h1 class="title is-h1">🐠 Tchang fish</h1>
  <div class="columns">
    <div class="column has-text-centered">
      <a href="{{ path('provider') }}" class="button is-primary is-large is-uppercase">Providers</a>
    </div>
    <div class="column has-text-centered">
      <a href="{{ path('supply') }}" class="button is-link is-large is-uppercase">Supplies</a>
    </div>
  </div>
{% endblock %}
